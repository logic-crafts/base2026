# Active Phase

Last updated: 2026-08-28

## Current override — product visibility and GitHub release live (2026-08-28)

Public Worker version `3f5a6687-4eb8-4ba5-9610-7fe2533282ba` is live with
immediate rollback `63d1f529-47ff-46ba-baeb-db77f6e80fc6`. The homepage now
uses the researched category “open video research engine / source-first
evidence library,” displays the verified live D1 snapshot (2,150 documents,
1,563 videos, 39 applied projections), and preserves the current cool-blue
`b26-independent-v1` shell on desktop and mobile.

The public roadmap now matches the live Cloudflare Workers, D1, R2, Workers
AI, Queues, Workflows, automatic projection, dynamic indexation, GSC and Bing
surfaces. The former VPS/local-ingestion story is removed from both the
fallback content and interactive roadmap data. The active growth phase is now
monitored indexation plus 10–15 strong source-backed evidence maps. The
DataForSEO decision packet was completed for `$0.077`; there is no arbitrary
fixed spend cap, but every request must remain decision-scoped and priced
before execution.

## Current override — SEO/GEO release live; measurement phase active (2026-08-28)

The technical SEO/GEO foundation is deployed on public Worker
`63d1f529-47ff-46ba-baeb-db77f6e80fc6` with rollback
`790e21d6-f341-4265-ae0c-7dc536a32495`. The extensionless canonical gate,
1,633-URL static sitemap corpus, 39-URL automatic dynamic sitemap, API and
public metadata checks pass. Google Search Console verifies the exact
`base2026.dev` Domain property, Bing imports only Base2026, both engines have
both sitemaps, and IndexNow accepted 57 changed URLs.

This earlier checkpoint is superseded by the completed `$0.077` positioning
packet and the current override above. Continue with measured GSC/Bing readback
and 10–15 useful topic maps; do not create thin doorway pages or repeatedly
resubmit unchanged URLs. Private capture operations continue under their
existing separate boundaries.

## Current override — verify repaired cloud capture drain (2026-08-26)

The active phase is operational verification of the private Cloudflare capture queue after fairness repair, not a local-Mac workflow. Private Worker version `555f0294-5530-4007-85af-d6e4a9639c4d` with migrations `0013` and `0014` is live. The five-minute schedule remains enabled; fresh zero-attempt TikTok sources must be selected before due retries, transient failures back off privately, and fourth failure becomes private source review. The historical incident is contained: exactly three rows were held, no public data or broad release gate changed.

## Current override — automatic Cloudflare-only publication is live (2026-08-23)

Public Worker `base2026` version `790e21d6-f341-4265-ae0c-7dc536a32495` is live with rollback `86faccf2-e986-4437-a39a-4b3d66a1883f`. Private Worker v0.6.1 `70fd6e68-ea54-462d-ba27-e3b1a66fa997` is live with pre-automatic rollback `f9e4a494-9780-4bd2-bb33-5b7f5a068f81`; migrations `0011` and `0012` are applied with none pending. Policy `base2026.machine-publication.v1` uses owner ref `owner-20260823-base2026-auto-publication-v1`, SHA `b37c900a03eb63252c7736c2197f2be1eae3f117eae76914f3cbef306d89e573`, batch10 and attempts4.

Dedicated automatic publication, import and projection are enabled; broad `PUBLIC_RELEASE_ENABLED=false` and local adapter false remain intentional. Discovery is `0 10 UTC`; reconcile/capture/automatic publication are `*/5`. The first run was attempted3/applied2/already_public1/retry0/held0/`hard_hold=false`; the two newly public IDs are `7271043105799834912` and `7402026836600851717`. Public post-counts are 2,136 documents / 1,557 distinct videos / 33 projection receipts / 44 cards / zero public full transcripts; private imports 35, applied projections 33, ready 0, automatic receipts 2 applied + 1 already_public, problems 0. API search found both rows, the site hash is unchanged, and replay attempted 0.

Latest partial discovery is 135/21/114/21/0/1 (discovered/fresh/duplicates/admitted/held/failed). AI is 308 completed / 18 pending / 43 held at 8,699 actual/reserved Neurons and 246 invocations, hard-blocked 0; monthly hard hold 0, browser hours approximately 0.01793 and container invocations 25. Pending jobs resume on the next daily budget and are not complete. Public34/34, private183/183, courier18/18, types/typecheck/dry-run pass. Valid eligible packets are automatic; malformed/privacy/mismatch packets fail closed. Observe the first full post-release discovery cycle at `2026-08-24 10:00 UTC` (not yet observed).

## Current override — cloud-only private v0.5.2 is live and draining held AI work (2026-08-23)

Private Worker `base2026-pipeline-control` v0.5.2 (`f9e4a494-9780-4bd2-bb33-5b7f5a068f81`) and migrations `0001`-`0010` are live. Cloud discovery/acquisition is authoritative: local adapter and both local LaunchAgents are off, scheduled Player acquisition uses the deployed private Container app, Browser Run/discovery and five-minute capture reconciliation are enabled, and the direct manual Container endpoint plus broad `PUBLIC_RELEASE_ENABLED` remain false. The 19-creator run admitted 21 of 135 discovered rows; capture proof completed 3/3 with no failure and the monthly ledger is below its 80% hard hold.

At the `19:56Z` checkpoint private D1 was 213 sources / 805 artifacts, with 32 imports and 31 exact public projections recorded. A subsequent `20:01Z` cron attempted 3 captures, completed 2 and left 1 retry; registry status moved to 12 captured / 9 admitted, media to 201, and five new transcription jobs are pending at the restored 7,500-Neuron cap. Public D1 remained unchanged at 2,134 documents / 1,555 videos / 31 projections / 42 cards / zero public full transcripts. Counters are timestamped and may drift on every cron; read the latest receipt before acting. Next action is automatic held-AI resumption after `00:00 UTC`, followed by the `10:00Z` discovery schedule; held packets still require explicit owner review/import/projection.

At `20:21Z`, capture of the new discovery wave was complete: 21/21 registry sources are `captured`, none remain `admitted`, media is 210, and 14 new transcription jobs are durably pending without additional AI spend. The only D1 `awaiting_capture` row is the older synthetic shadow control. Monthly reservations are zero and `hard_hold=0`.

Four semantic jobs held by the superseded native-schema provider error are conditionally pending for `2026-08-24 00:00:00` with zero attempts and four audit receipts. They will join the normal UTC-budget drain; deterministic evidence/transcription review holds remain untouched.

## Current override — bounded daily intake is complete and waiting on UTC AI reset (2026-08-23)

The live private Worker remains v0.4.2 `c8ee1126-6eb9-4129-a75d-89f78a481cb0`. Reconcile and maintenance returned zero dispatches, recoveries, resumes and dead letters. A fresh 245-row discovery receipt yielded one exact unseen ID; it was explicitly admitted, staged audio-only, uploaded with its manifest, and durably queued for transcription. Workers AI is at 7,494/7,500 Neurons, so the new source is correctly held with `ai_soft_cap` and no same-day rearm.

There is no ready semantic-pass source for this Luna run. No private import or public projection occurred; public Evidence remains 2,134 documents / 1,555 unique TikTok videos / zero full transcripts, and the fresh unseen remainder is zero. Continue from Cloudflare receipts after the next UTC reset, keeping broad release, Container, AI Gateway and paid fallback disabled.

## Current override — repaired live pipeline is draining normally (2026-08-23)

The live verification/fix pass is closed. Private Worker v0.4.2 `c8ee1126-6eb9-4129-a75d-89f78a481cb0` recovered the stranded normal Queue job and the 31 Workers AI jobs that had exhausted attempts while bouncing against the same-day soft cap. Daily-cap overflow is now a durable zero-attempt hold until the next UTC reset. Tests, typechecks, dry-runs, signed health, reconcile/maintenance, D1/R2 hashes, outboxes and DLQs are green.

Fresh discovery admitted eight exact IDs and local canonical is 4,101. Seven uploaded successfully; one exact source remains in the retry lane after stable yt-dlp `2026.08.19` still returned no formats. Two Luna Max batches reviewed 20 ready sources, passed/projected eight and held twelve. After removing three legacy contact-bearing passages, public Evidence is 2,134 documents / 1,555 unique TikTok videos with 31 applied projections and zero public full transcripts.

Normal operation remains the active daily 06:00 Luna Max run: resume durable Cloudflare work first, drain all ready semantic batches, then refresh and admit the full exact unseen set in bounded waves. All private local directories/files must remain `0700`/`0600`. Broad release, Container, AI Gateway and paid fallback remain disabled.

## Historical override — batch Cloudflare video pipeline v0.4.0 checkpoint (2026-08-22)

The implementation phase is closed and batch operation is live. Public Worker `base2026` version `86faccf2-e986-4437-a39a-4b3d66a1883f` and private Worker `base2026-pipeline-control` v0.4.0 version `831bff85-cae3-4e13-98e1-d699a9b64615` use exact private-to-public service-binding projection. Public/private migrations are `0003`/`0009`; `PUBLIC_PROJECTION_ENABLED=true` and broad `PUBLIC_RELEASE_ENABLED=false`.

All 210 unseen candidates from the current 245-row discovery snapshot were admitted privately. Audio-first staging and one retry uploaded 178; 32 remain private retry/manual-source work. Luna reviewed 31 completed Cloudflare packets, passed 22 and held 9. All 22 admitted sources are live and search-verified. Public Evidence is 2,129 documents / 1,548 unique TikTok videos; local canonical is 4,093 and snapshot unseen remainder is zero.

Normal operation is one daily 06:00 Luna Max automation that resumes durable Cloudflare work first, admits the full fresh unseen set through repeated waves of at most 100 IDs, transfers in 10-source/50-MiB chunks, and processes every currently ready semantic batch. Workers AI stops below included daily capacity and resumes after the UTC reset. Container, AI Gateway, paid fallback and broad public release remain off.

## Current override — private Cloudflare evidence pipeline v0.3.1 is live (2026-08-22)

The isolated private control plane is live at `base2026-pipeline-control.offflinerpsy.workers.dev` on Worker version `905ae9e4-fe0f-47fb-b3c1-06dd6bfe7319`. It uses separate D1 `base2026-pipeline-private`, private R2 `base2026-pipeline-artifacts`, two primary Queues plus DLQs, `Base2026PipelineWorkflow`, a five-minute reconciler, and Workers AI behind D1 soft/hard daily Neuron gates. Migrations `0001`-`0007` are applied. HMAC intake/admin secrets remain in Cloudflare Secrets and the local macOS Keychain.

The production-boundary E2E remains `imported_private`: exact release receipt `072a76060006a4212889af4ccc368c616fa30183`, materialization `a6c7ea121154b043253337b72ff11e1e005f8627`, one held-private card, all public flags `0`, duplicate review/import idempotency, contradictory-review `409`, and exact R2 SHA-256 readback. A separate v0.3.1 live proof ran exact model `@cf/meta/llama-3.1-8b-instruct-fp8-fast`: one completed semantic invocation reserved/accounted 22 Neurons and produced R2 SHA-256 `7b4d0594f3473f4700b48b13d0e98577ef46c00ccfdf94b91909778b73d556aa`. Current ledger is 4,778 Neurons / 4 completed invocations, hard-block `0`. `PUBLIC_RELEASE_ENABLED=false`, Container capture remains off, and ChatGPT is available only through the manual filesystem courier.

Local zero-model-token automation is loaded as LaunchAgent `com.base2026.cloudflare-pipeline-adapter`: 15-minute interval, last exit `0`, current work order `idle`, secrets read from Keychain, private directories/files normalized to `0700`/`0600`, a 1 MiB fail-closed work-order cap, and no public release operation. The public `base2026` Worker remains `806e35c7-f230-4e77-aeea-15169b4faaf0`; public Search/Outreach/Inbox counts remain 2,095 / 78 / 0 and the homepage SHA-256 remains `696c473bc5bf1a93ecb01e140100edc9019f8efc5c8ff3f5a9b29ddc6acdf98d`.

The implementation phase is complete. Normal operation is bounded private intake and monitoring. Any public projection remains a separate owner-authorized task outside this control plane.

## Current override — curated Outreach search is live (2026-08-21)

The owner-authorized Outreach v1 release is live at `https://base2026.dev/workspace/` on Worker `806e35c7-f230-4e77-aeea-15169b4faaf0`. Workspace exposes `All`, `Evidence`, and `Outreach findings`; the combined view keeps the two collections independently labelled and ranked. The previous Worker `34bdf3fe-90f8-4580-a88b-d39503655818` is the immediate rollback.

The private 34-tab workbook remained read-only. A semantic review reduced 400 mechanically eligible candidates to 78 high-fit findings: 37 AI-search/GEO/SEO, 31 agent/workflow-governance, and 10 research/tool/organic-distribution records. Private GSC/GA4, Search Ops, ACQ3, Gmail, client, contact, queue, formula and owner-only surfaces remain excluded. Future workbook additions are not automatically public.

The live collection uses separate D1 `base2026-outreach-search` (`ffbef187-67ef-491f-8fae-62f625636ed5`) through `OUTREACH_DB`; the existing Evidence and Inbox databases remain isolated. Remote readback is 78 findings / 78 FTS rows / 83 topics / 86 lanes. Desktop and mobile live QA passed with zero JavaScript errors or horizontal overflow; the Evidence control query still returns 40 `schema` hits. The phase is complete. Canonical receipt: `BASE2026_OUTREACH_PRODUCTION_RELEASE_2026_08_21.md`.

## Current override — independent Base2026 design recovery and Google Workspace email live (2026-08-20)

Owner-authorized `b26-independent-v1` recovery plus the r10 iPhone scroll hotfix is deployed at `https://base2026.dev/` on Worker `34bdf3fe-90f8-4580-a88b-d39503655818`. The hotfix removes the root-only body `overflow: clip` that blocked WebKit scrolling; live iPhone emulation now scrolls, the mobile menu opens and closes, and horizontal overflow remains zero. The live system continues to use one Base2026-only cool-blue shell and core component contract across Home, Workspace, indexes, info pages, forms, solutions and detail pages. Protected D1 search/forms, canonical/robots/redirects, public records and attribution remain intact.

Email is live through the existing paid Google Workspace Business Starter account. `hello@base2026.dev` is the primary Gmail identity and `offflinerpsy@gmail.com` remains usable as a sender/recipient in the same mailbox. Public DNS is Google-only for mail: MX `1 smtp.google.com`, SPF `include:_spf.google.com`, 2048-bit DKIM selector `google`, and DMARC monitoring at `p=none`. Cloudflare Email Routing is disabled; do not re-enable or mix its MX records with Google. Workspace reports Gmail activated and DKIM accepted. Same-account two-direction checks passed, and independent AboutMyEmail receipt `27aeac2d` reports SPF aligned, DKIM aligned and DMARC pass. The mail cutover gate is closed. Preserve immediate Worker rollback `48d8ea7e-f9db-464c-a173-265ab991fc24` and older rollbacks `3c12d5a3-5855-4971-b163-9d5b067e8031`, `237385ff-5984-44c8-8df3-b52873249296`, `c5e88c7f-707b-4572-8d33-e369eecb2bb7` and `4389e513-c16a-4bcf-9f8c-b97ac55b7825`.

## Historical override — independent Base2026 design recovery audit complete (2026-08-20)

The domain/runtime separation remains live and protected. A source-level audit has confirmed that the public site mixes a startup/Superdesign Home with a legacy generated corpus. The active product task is now the isolated `b26-independent-v1` design candidate described in `BASE2026_DESIGN_RECOVERY_AUDIT_2026_08_20.md`. It must unify the Base2026 header, footer, tokens, controls and responsive component system through generators and the release builder, without reintroducing Alex Personal authority or changing protected search, form, data, canonical, redirect, indexability or attribution behavior.

This audit made no product or infrastructure mutation. The candidate must start from clean publication commit `de96c08f8f5e28f3ac0ce5236093b4f0b5c152e9`, preserve the dirty canonical checkout, and remain local until fresh full release, visual, functional and public-boundary evidence passes. Existing Worker rollback versions `c5e88c7f-707b-4572-8d33-e369eecb2bb7` and `4389e513-c16a-4bcf-9f8c-b97ac55b7825` remain intact.

## Current override — cross-machine continuity confirmed; standalone startup release remains live (2026-08-20)

The active Base2026 product is the standalone public startup at `https://base2026.dev/`, not the former `/knowledge/` experience on Alex Personal. It runs on Cloudflare Workers Static Assets with D1 FTS5 public search; `/workspace/` preserves the search workspace, `/search*` redirects to it, and `www.base2026.dev` redirects to the apex. Alex Personal WordPress remains separate at `https://aggressorbulkit.online/`; no personal-services navigation, forms or links belong in Base2026.

Cloudflare Worker version `237385ff-5984-44c8-8df3-b52873249296` is the live startup release. Its public search D1 is `base2026-public-search` (`ac034130-4169-43c2-9a17-4b72d05457b0`); Support and Partner proposals use separate private D1 `base2026-inbox` (`542a77ef-da00-4522-8b7a-3d78fc646c72`). Preserve both Worker rollback versions `c5e88c7f-707b-4572-8d33-e369eecb2bb7` and `4389e513-c16a-4bcf-9f8c-b97ac55b7825`, the VPS release tree and Meilisearch until a separate stability decision.

The product/infrastructure phase is complete. The only current work is external GitHub recovery: GitHub billing currently blocks CodeQL before steps run, and the legacy GitHub Pages deployment endpoint returned HTTP 500 despite successful builds. Do not change Base2026 code, Cloudflare, D1, DNS, deployments, branches or GitHub settings to work around those external failures. The next safe action is to resolve the GitHub billing lock, rerun CodeQL, then retry the legacy Pages deployment only after GitHub's deployment API recovers.

## Current override — startup domain separation and Cloudflare preview planning (2026-08-19)

Base2026 content intake, public-data expansion, UI redesign and VPS production release activity remain paused/frozen. The newly active tightly scoped phase is infrastructure separation: prepare a preview-only Cloudflare Workers Static Assets plus D1 FTS5 candidate for `base2026.dev`, while keeping Alex Personal WordPress unchanged at `aggressorbulkit.online`. The zone is active on Cloudflare Free but has no apex/`www` web answer or attached Base2026 Worker. No DNS, custom-domain, nginx, Meilisearch, production deploy, commit, push, intake or indexation change is authorized from the planning audit. Canonical plan: `BASE2026_CLOUDFLARE_DOMAIN_MIGRATION_PLAN_2026_08_19.md`.

## Current override — R6 production closed (2026-07-16)

Exact R6 is deployed and live-verified; task `83` is `done`, task `10` remains `ready`, and task `54` remains `scoped` behind task `10`. The earlier `base2026.com` owner-boundary alert was an unsupported agent inference and is retracted: that domain is not part of the project and requires no owner action. Canonical production remains `https://aggressorbulkit.online/knowledge/`.

## Current override — independent Source Detail V2 release-boundary closure

The isolated Source Detail V2 contract/determinism/footer visual gate is complete and green. Canonical admission is closed: all 13 post-freeze emitted sources are terminal `future_private_backlog`, absent from isolated public artifacts, and public scope remains 1,692 source records. Sitemap is closed as a false positive: the canonical `solutions/content-refresh-prioritization.html` appears once locally/live; `...-framework.html` is not a canonical path. The active phase is now scoped Git/publication ownership recapture plus an independent go/no-go permit over an immutable candidate. Package/deploy is allowed only after both are green; otherwise stop with evidence-backed NO-GO. Production remains unchanged at this override.

## Current active phase

Public product architecture correction plus launch monitoring and check-only TikTok intake pipeline hardening.

## Why this is active

The public intelligence layer is deployed, GitHub publication is complete, WordPress lead-flow is live, and the current source-with-passages/no-card backfill queue is closed. The immediate priority is to correct the public product contract so the UI behaves like the intended searchable video-source database, while continuing launch/indexing/analytics monitoring and local-first TikTok/source pipeline work through reviewed gates.

## Current exact task

Operate the Base2026/geo launch command center through scoped workers: validate deployed Ahrefs/GSC launch fixes with local crawl gates, mine the public Base2026 dataset for practical SEO/content/growth work, and run TikTok/source refreshes only through the canonical release gate. The current live Base2026 static release is `base2026-ai-ready-documentation-page-20260626`; the latest data/reindex checkpoint is `base2026-source-review-alex-approved-remaining-ay58-20260624`. The local social-discovery bridge is implemented and proven through live releases: `scripts/social-discover.py` writes ignored private JSONL, `scripts/import-social-discovery-to-tiktok-csv.py` dry-runs/applies TikTok-only rows into ignored private `videos.csv` with backup and dedupe, and `scripts/base2026-release-gate.ps1 -LatestReadiness 3` owns public export, deploy, reindex, and live QA for fresh source batches. `scripts/hermes-tiktok-refresh.ps1 -AfterPolish` now skips inventory/caption intake and only rebuilds from existing reviewed polish outputs, preventing accidental queue expansion during release packaging.

The current live export has 1476 source records, 2016 passages, 1631 insight cards, 1060 public insight cards, 1522 topics, 1008 public topics, and 10 creators. Current static release is `base2026-ai-ready-documentation-page-20260626`; latest data/reindex checkpoint remains `base2026-source-review-alex-approved-remaining-ay58-20260624`. The AI Recommends Solutions creator pass is closed live: 100 TikTok candidate rows were imported from ignored private discovery output, 77 polished transcripts were reviewed, additional public-ready sources are represented after ASR recovery, readiness cards, and twenty-four explicit local-caption source-review cleanups, and the remaining uncertain rows stayed private/gated. ay43 briefly showed why single-source readiness was too narrow; ay44 added two exact-evidence `@gobigsystems` Source Intelligence cards, ay45 kept weak/no-speech ASR rows private, ay46 confirmed `-LatestReadiness 3` must remain active for every fresh source batch, ay47-ay53 proved the guarded `tiktok-clear-reviewed-source-rows.py` transition for explicit QA-pass source-review rows, and ay54 fixed the source-detail Q&A contract: source Q&A renders only from reviewed Source Intelligence, not from raw source text fallbacks.

Future creators and fresh TikTok refreshes must enter through ignored local config, private discovery JSONL, importer dry-run/apply, current-batch polish gate, newest-source readiness, and then the canonical release gate. The source-detail contract keeps reviewed `public_source_text`, `source_summary_short`, and `source_summary_long`, keeps legacy `transcript` empty, collapses reviewed evidence inside Source Intelligence cards by default, suppresses duplicate/fragmentary source-evidence disclosures that are already present in Source Text, preserves readable paragraph breaks and chunks long ASR/TikTok source text, suppresses duplicate lead/summary copy in source detail, keeps compact runtime source share/meta controls, adds Source Intelligence share/copy/print controls, renders "Questions this source answers" only from reviewed Source Intelligence cards, blocks newest source-only records with no topics/public insights during release packaging, keeps public info pages/roadmap/support aligned with the corrected product passport, exposes `/knowledge/api.html` plus public AI/API entry points, emits shared OG/X social metadata for indexable generated/info/search entry pages, keeps exactly one visible H1 in hydrated source-detail views, keeps search-result identity links and TikTok outbound source links, keeps shared Alex Yarosh favicon/touch-icon assets, and keeps the footer `Socials` row with working X/GitHub links plus a disabled TikTok placeholder. Clean rebuild now replays ignored reviewed legacy/candidate insight archives. Current transcript status is 1476 transcribed public-source records with source-review debt gated. Remaining source-review debt is 36 private rows: 21 local-caption rows needing source/QA review, 13 audio-backed too-little/no-speech rows, and 2 rows without usable local caption/audio.

## Important note

Generated public pages should not be committed by default; prefer reproducible generation from scripts. Public deploys are allowed only after export policy, publication boundary, and live QA pass.
