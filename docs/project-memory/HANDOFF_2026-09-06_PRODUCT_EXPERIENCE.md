# Product experience — published release

Checkpoint: 2026-09-06. The source-linked homepage, investor overview,
public roadmap, methodology, source policy and English 3D factory are live.
The final Astra/max correction also delivers the rebuilt Founder page,
illustrated shared menus, mobile navigation and editorial social footer.

## Published baseline

- Worker: `138e8cd6-b85b-49e6-9d59-e7ca370412e4`, active at 100 percent.
- Main module SHA256:
  `24afbe0e1ff23eb9fc9fff38fdb6dabcd8e81bca83cacf8c3293cc3f2d7f49a1`.
- Artifact: `output/product-experience-release-v2`, 4,339 served files,
  135,855,767 bytes; tree SHA256
  `748cff2b549eb2af36bcf881705795b6144c7e4bd81408d44cb935ab5f46afbd`.
- Public Factory manifest SHA256:
  `c8417b186a68b499b13685fd1e431b76f6ba67e52066e339134a7ea597c413d3`.

The reviewed public assets were combined with the accepted private runtime.
The source-catalog validator accepts a complete class token within one class
attribute and continues to reject malformed shells. Auth, bindings, member
assets and both WordPress ZIPs remain preserved. Recovery and diagnostic
controls are off; no schema or member-data mutation was performed. The public
checkout alone is not a substitute for the combined private integration.

## Design and asset boundary

Founder uses a generated portrait based on the supplied saved image, real
public Dreamwood and Aster website captures, native role disclosures and
specific links to selected work. Five social channels use one generated art
atlas. Four new raster files are admitted by exact path, size and SHA256;
unknown files, symlinks and altered bytes are rejected. The image-generation
tool does not expose a named model selector. Private portrait inputs, prompts
and operational QA remain outside the public release.

The desktop header has two illustrated menus plus direct tool and investor
links. Mobile navigation exposes the major destinations and restores focus
when closed. The footer keeps all five social channels, four actual directory
links and trust pages visible. Header offsets are 81px desktop and 71px on
mobile, including the border. Founder disclosures work without JavaScript;
entry animation stops under reduced motion.

## Verification

The final source passed 67 focused Python checks and 13 JavaScript behavior
checks. The combined runtime passed 282 affected tests, 27 member tests and
TypeScript. The public artifact privacy gates and explicit-assets dry run
passed. Fifty production files, including all twenty Factory files and all
four new raster assets, match the reviewed artifact exactly. Module and
version attribution match; signed-out session and collections responses keep
their private headers. The publication audit is repeated before source commit.

Native desktop/mobile review covered the final Founder composition, menus,
focus, overflow, public case disclosures and footer art. Prior unchanged
homepage, Blog/Topics, attributed-source and 3D controls retain their verified
behavior. The factory uses nine licensed models and three textures; its
candidate passed keyboard, camera, visit, scenario, reduced-motion and
WebGL-failure checks. Full receipts and screenshots remain in the private studio.

## Boundaries and continuation

The public Factory is a fixed authored scenario. A separate loopback view
reads the existing AgencyOS registry; its adapter, private endpoints and
snapshots are not in public source or assets. Animation does not prove that
an agent is running. A future independent AgencyOS product is exploratory.

Astra/max owns design, architecture, engineering and substantive review.
Ordinary supervision uses Astra/medium; routine workers use Luna/max, with
Sol/medium or Terra/medium for bounded intermediate work. Chief maintains the
single AgencyOS registry. Phase 29 and external adoption work remain active;
continue from NEXT_ACTION without repeating completed deliveries.
